<Window x:Class="Goguma.Logger.LoggerUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Logger" Height="350" Width="600" WindowStartupLocation="CenterScreen"
        Background="Black" Foreground="Gray">
    <Grid>
        <RichTextBox x:Name="tbLog" Background="Black" Foreground="Gray" Focusable="False" Cursor="Arrow"
                     VerticalScrollBarVisibility="Visible" TextChanged="OnLoggerChanged" FontFamily="Consolas">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0.1" />
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>
    </Grid>
</Window>